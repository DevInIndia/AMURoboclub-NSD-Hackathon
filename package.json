{
  "name": "amu-roboclub-nsd-hackathon",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n \"FRONTEND,BACKEND\" -c \"cyan,magenta\" \"npm run dev --prefix frontend\" \"npm run server --prefix backend\"",
    "frontend": "npm run dev --prefix frontend",
    "backend": "npm run server --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
